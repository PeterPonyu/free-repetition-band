name: visual-pixel

# Optional / gated until portal/reference.png is user-approved.
# Must not fail required CI (job if: false).
on:
  workflow_dispatch:
  pull_request:

permissions:
  contents: read

jobs:
  visual-ralph-pixel:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "Visual Ralph score>=90 / pixel-diff is gated until reference approval."
